---
unit: 1
title: My words
pos: noun
---

<!--
PASTE YOUR WORDS BELOW.

The only thing a line needs is Hebrew, an equals sign, and English:

    - שלום = hello

Everything else is optional. Niqqud is optional too - without it the app
just won't show a transliteration, and will say so rather than guess.

Group words under `##` headings. Each heading becomes one lesson on the path,
so put things together that belong together. Aim for 4-8 words per heading;
smaller groups get merged with the next one, bigger ones get split.

`pos: noun` in the frontmatter above is the default part of speech for this
file. Change it, or override per word with `; pos: verb`. A heading that names
a word class (## Verbs, ## Adjectives) sets it for that section.

Useful extras, all optional, each after a semicolon:

    - כלב = dog ; gender: m ; root: כ-ל-ב ; ex: הכלב קטן | The dog is small

When you're done:

    npm run content:check

That reports anything it couldn't parse, and lists what it had to guess so you
can confirm or correct it. If a word here duplicates one from the starter files
it will tell you which file and line, so you can delete whichever you prefer.

Delete this comment block once you're going.
-->

## My first words
